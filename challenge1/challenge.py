name = input("what is ur mane M8?")

age = input("how old r u fam?")

username = input("wat is ur username?")

answre = "your name is %s, your age is %s, your username is %s." % (name, age, username)

print(answre)